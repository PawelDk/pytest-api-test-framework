[pytest]
testpaths = tests
addopts = -v --tb=short --alluredir=allure-results --junitxml=reports/junit.xml
pythonpath = .
markers =
    live: test exercises the real API over the network (deselect with -m "not live")
